// // TotalQuantity.js
// const calculateTotalQuantity = (productsWithQuantity) => {
//   if (!Array.isArray(productsWithQuantity)) {
//     return 0; // or handle the default case as needed
//   }
//   return productsWithQuantity.reduce((total, product) => {
//     return total + (product.quantity || 0);
//   }, 0);
// };

// export default calculateTotalQuantity;
